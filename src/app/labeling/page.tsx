import LabelingTool from "@/components/labeling/LabelingTool";

export const metadata = {
  title: "Dataset Labeling — Recipe Box",
};

export default function LabelingPage() {
  return <LabelingTool />;
}
